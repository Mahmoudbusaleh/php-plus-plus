# 1. منع استعراض الملفات داخل المجلدات لأغراض أمنية
Options -Indexes

# 2. تفعيل محرك إعادة الكتابة
RewriteEngine On

# 3. التأكد من أن الطلب ليس لملف حقيقي (صورة، CSS، إلخ)
RewriteCond %{REQUEST_FILENAME} !-f
# 4. التأكد من أن الطلب ليس لمجلد حقيقي
RewriteCond %{REQUEST_FILENAME} !-d

# 5. توجيه كل شيء لملف index.php مع الحفاظ على البيانات (QSA)
# الـ [L] تعني أن هذا هو آخر سطر يتم تنفيذه
RewriteRule ^(.*)$ index.php [QSA,L]
